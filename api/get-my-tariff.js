const{adminClient,authUser}=require('../lib/api-helpers');const CUTOFF=Date.parse('2026-07-12T14:57:29Z');module.exports=async(req,res)=>{if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const db=adminClient(),u=await authUser(db,req);if(!u)return res.status(401).json({error:'Accesso richiesto'});const{data}=await db.from('client_tariffs').select('tariff_mode').eq('user_id',u.id).maybeSingle();const mode=data?.tariff_mode||((Date.parse(u.created_at)<CUTOFF)?'storico':'piena');res.json({mode})}catch(e){res.status(500).json({error:e.message})}};